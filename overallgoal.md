# Payne Zero II — 大局分析

写于 2026-08-13。这是纲领层面的判断，不是任务清单；执行计划另见
`~/.claude/plans/evaluate-empirical-claims-about-encapsulated-tower.md`。

所有数字取自 `results/*.json` 与 `paper/numbers.tex`，不取自 `notes/` 的叙述文字
（`paper/README.md` 自己警告过 `notes/reduced_state_progress.md` 混了至少八个被否决的
checkpoint）。

---

## 一、最核心的问题：标题和 Results 讲的不是同一篇论文

`paper/main.tex` 的标题是 *Two fields are enough*——一个关于**表示**的物理论断。但 Results
六个小节里有四个在讲**那个学出来的网络**：它多快、它在盲测里怎么失败、它的 gate 怎么定。

两篇论文的骨架被塞进了一个躯干：

- **A：表示论文。** 1D LTE 大气每层状态是二维的；把其余四场交给 solver 自己的
  EOS / 不透明度 / 流体静力闭合去重建，它们按构造自洽而不是按训练自洽。推论是给所有做大气
  emulator 的人的：**别预测六场**。
- **B：加速器论文。** 一个学出来的两场 initializer 比已发布的六场网络快 27%
  （`5.59` → `4.07` 轮）、非单调轨迹从 `53.3%` 降到 `18.3%`。

A 的证据强度远高于 B，而且 A 不依赖任何网络。但目前篇幅分配是 B 占主导。这是全篇最大的结构
问题，比任何一张图都重要。

顺带：A 的物理内容对写过大气程序的人接近"当然如此"——给定 `(m,T)` 和成分，`P`、`n_e`、`κ`
本来就由 EOS 决定。**论文的真正贡献不是发现这件事，而是三点**：

1. 量化它在真实数值下（固定 τ 网格、顶边界、fp32 Λ 迭代）依然成立——重建场到几倍
   `1e-4`，光谱中位差 `8.05e-5`，0/60 越过 `5e-3`；
2. 证明 **no-remap 是承重条件**，并因此解释了前人两场交接失败的负面结果；
3. 由此得出的 emulator 设计推论——六输出网络给出四个各自近似正确、彼此不自洽的场；
   两输出网络加物理路径给出四个按构造自洽的场。

第三点才是 payload，现在被埋在 Discussion 里。

---

## 二、最大的战略风险：插值 baseline 决定这是 A 还是 B

有网格的时候，没有生产代码从 Eddington-grey 起步；MARCS/ATLAS 的用户**插值已有模型网格**。
grey 是地板，不是经典方法的现状。

如果一个朴素的近邻插值在收敛上就接近那个网络，那么：

- **B 塌掉一半。** 网络的价值从"更好的物理先验"退化为"一个不用带 50k 模型网格的紧凑代理"
  ——那是部署论证（内存、分发），不是物理论证。
- **A 毫发无损，甚至更强。** 插值同样受益于 rematerialisation，等于多一个独立证据支持
  "reduced 坐标 + 物理重建"这条路线。

所以插值臂不只是补一个 baseline，它是**决定论文该叫什么名字的实验**。应当在写作正式开始之前
拿到这个数，而不是等全套战役收尾。

已知的两个事实（2026-08-13）：插值起点在**剖面空间**比网络差 5–9 倍
（T p95 `3.12e-2` vs `3.38e-3`；m p95 `1.39e-1` vs `2.66e-2` dex）；但真实 solver 上跑的
第一颗星 **3 轮收敛，落在地板上**。n=1 不能下结论，但它指向的方向恰恰是论文自己的核心论断
（剖面接近度不预测收敛），所以这个风险不会自己消失。

---

## 三、关于"全部折入"这个 scope 决定

**折入之后，论文的结局挂在一个已经失败过一次的封存测试上。** 而这次改进的主要新成分是
**gate**——检测并回退。gate 不让网络变好，它让失败变得可检测。

真实失败是 column mass 在基座网络里（m p95 `2.66e-2` dex vs 限 `1.52e-2`，18 颗 mass
blowout；温度 p95 `3.38e-3` 在其 `3.74e-3` 限内）。改进计划里针对它的只有 stage 4 那个
bounded correction，而上一次同类 correction 在样本外被证明**完全惰性**——K=1 adapter 加
rescue 把盲测星中位移动了 `1.0e-14` dex，把 18 颗 blowout 星移动**恰好 0**。

最可能的结局：封存测试"通过"，但通过方式是回退率足够高以致没有 blowout，而迭代增益被回退比例
稀释。仍可发表，但比 A 弱得多，且多押了几周和一次不可重来的开封机会。

**判断：这是两篇论文，合并让两篇都变差。** A 现在就能投且几乎不可反驳；B 需要的是真正修好
column mass，不是给它加个 gate。

决定已经拍板为合并，按此执行。但**若封存测试再失败一次，应立刻拆回两篇，而不是去找第三个
封存集。**

---

## 四、被低估的资产：solver 自身的不动点宽度

`5e-3` 那条杆的校准实验里有个数字，比 initializer 结果更值得被引用：

> 同一个 solver，从两个 production 都愿意出结果的起点跑，收敛后的光谱中位差 `3.44e-3`，
> 最大 `1.50e-2`，56 颗里 16 颗越过 `5e-3`。

这不是关于网络的陈述，是关于**模型大气本身可复现性**的陈述——两次合法运行给出的光谱差
0.3%–1.5%。对做高精度丰度的人（目标 0.01–0.05 dex）这不是可忽略的量级。

目前它躺在 `main.tex` §5.6 当一个 calibration control。**应提升为独立结果甚至独立小节。**
它的受众比 initializer 大得多——每个用模型大气的人都该知道这个数——而且它顺便把全篇所有光谱
数字变得可读。这是全篇最容易被引用、目前却最被浪费的一个测量。

限度要写清：n≈56–60，单一代码，单一 retry 策略。

**这个数还有一个直接的推论**：`25/189` 光谱越界不是二场的失败证据。production 从自己的
retry start 重启越界 `54/192 (28.1%)`，candidate 是 `25/189 (13.2%)`——candidate 越过的
那条杆，production solver 自己越过的频率是它的两倍。把它当不合格证据，是把一个已被证明不是
绝对量的阈值当绝对阈值用。

---

## 五、纲领层面：可微 twin 这条线还活着吗

T1–T8 是很大的工程投入。唯一一次真正检验它的场合，post-mortem 显示 adapter 在样本外位移是
`1e-14` 量级——**目前没有证据说它有效**。这也意味着那次盲测**根本没有测到
solver-in-the-loop**：失败和 `4.14`-vs-`5.68` 的速度优势都属于 base 两场 checkpoint。
（这一点必须写进 §5.7——现在那里把冻结策略描述成三件套，其中两件可证明惰性。）

这是一个需要显式决策的岔路，不该默认继续：

- **若继续**，先做一个最小的可证伪实验——K 提到 3–5，或换成全局而非 RBF 的修正——目标只有
  一个：**让 adapter 在样本外产生非零位移**。连这个都做不到就停。
- **若停**，把 T1–T8 写成方法/软件说明或附录，回收沉没成本。

顺带：原始动机（学习阻尼控制器）已被自己的数据削弱——好起点下 non-monotonic 从 `64%`
（均匀标签盒）降到 `18.3%`。这条线也该正式关闭或重新论证。

---

## 六、真正限制这项工作科学价值的，是语料

论文自己的 limits 写了，但权重被低估：**语料只含收敛的大气**（准入契约是 30 轮内解出）。
于是全篇每一个收敛结论都条件于"solver 本来就解得出的星"。

最有意思的那个问题——**更好的起点能不能救回本来解不出的星**——用这个语料无法回答。

这是把工作从"我们让容易的星更快"提升到"我们扩大了可解域"的分水岭。后者对 survey pipeline
的意义大一个量级，也是让这篇被反复引用的唯一路径。代价是要生成一批**包含失败**的尝试语料，
这是新的 solver 运行，不是重分析。

grey 实验其实已经无意中触碰到这件事：grey 起点让一批星解不出来。**把它反过来用**——从难解的
起点出发，看哪个 initializer 能把星拉回可解域——就是这个实验的雏形，机器已经有了。这可能是
整个纲领里性价比最高的下一步。

（同时注意 grey 结论的一处过度解读：smoke test 里有星在第 22 轮收敛，43 颗补跑救回 10 颗。
一部分 grey 失败是"在盆里但比 production cap 慢"，不是"在盆外"。应三分类报告：
收敛 ≤15 / 收敛 16–30 / 30 轮仍失败，**只有第三类支持吸引域断言**。）

---

## 七、决策顺序

1. **先拿插值臂的收敛数**（几十核时）——它决定摘要怎么写。
2. **决定 A/B 是一篇还是两篇**，在写作开始前，不在封存测试之后。
3. **把不动点宽度提升为独立结果**——免费，数据已有。
4. **给可微 twin 一个可证伪的最小实验**，并设定停止条件。
5. **规划失败语料**——这是下一篇的地基。

前三条都不依赖还在跑的战役。它们比等 400 星和 grey 收尾更紧急，因为**它们改变的是论文的形状，
而战役只改变论文里的数字。**

---

## 附：几个必须修正的表述

- 迭代直方图是 `{3:56, 4:1, 7:1, 9:1, 12:1}`。truth `(m,T)` 的 `3.33` vs 六场 `3.23`
  之差**全部来自 4 颗尾巴星**，不是系统性惩罚。应报 floor count（56/60 vs 57/60）+ median，
  mean 放括号。只报 mean 等于邀请审稿人问"所以还是慢了"。
- 60/60 应写成 **"≥94% (Wilson, 95%)"**，不是 100%。
- 配对迭代差用 **Wilcoxon signed-rank**，不用 t 检验（小整数、强偏斜）。137 faster /
  20 slower 的符号检验 z ≈ 9.3，p ≈ 10⁻²⁰——直接引用这个数。
- "19 个剖面异常"应改为**单一坐标失败**（column mass）。这个定位才是可操作的，它同时告诉你
  要修什么、gate 该盯什么。
- 400 星与新 60 星的数字目前**不在本地 checkout**，在 `astronode-garching-gpu` 上。
  拉回 + hash + `collect_numbers.py --check` 通过之前不引用。400 星是**已开封的校准集**，
  其中 300 颗要拿去训练 gate——基座网络在 400 上的收敛率是合法样本外数字，但任何 **policy
  层面**的数字在那 300 颗上是样本内的。**只有封存的 200 能承载泛化断言。**
- 新 60 星的光谱数字**在没有同批 jitter control 的情况下不可解释**。dev-60 上 control 是
  16/56 越界；若新 60 上 control 是 12/60，那 4/60 是**赢**。
